<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/Tofu.png" type="image/png" />
    <title>後台管理</title>
</head>
<style>
    * { margin: 0; padding: 0; box-sizing: border-box;
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI",
                    "Noto Sans", Arial, sans-serif; }

    body { display: flex; justify-content: flex-start; align-items: center; flex-direction: column;
       width: 100%; min-height: 100vh; padding: 40px 20px; overflow-x: hidden; }

    .container { width: 100%; max-width: 450px; }

    .editOrdertitle {  padding: 32px; box-shadow: 0 20px 40px rgba(0, 0, 0, 0.08);
        margin-bottom: 15px; border-radius: 14px; }

    .title-row { display: flex; align-items: center; justify-content: space-between; }

    .title-row h1 { margin: 0; font-size: clamp(24px, 5vw, 32px); }

    .back-btn { margin: 0; white-space: nowrap; }

    button { width: auto; min-width: 80px; padding: 8px; background: linear-gradient(135deg, #f34f4f, #d63636); color: white; margin-bottom: 15px;
             border: none; border-radius: 8px; font-size: 15px; cursor: pointer; transition: transform 0.1s, box-shadow 0.1s; }

    button:hover { box-shadow: 0 8px 20px rgba(54, 107, 214, 0.35); transform: translateY(-1px); }
    button:active { transform: translateY(0); box-shadow: 0 4px 10px rgba(54, 107, 214, 0.25); }

    .order-card { border-radius: 8px; padding: 25px;  margin-bottom: 20px; position: relative; box-shadow: 0 4px 15px rgba(0,0,0,0.05); }

    .order-card h2 { font-size: 18px; margin-bottom: 12px; border-bottom: 1px solid #eee; padding-bottom: 8px; }
    
    .order-card p { font-size: 14px; margin-bottom: 6px; }

    .order-card h3 { font-size: 18px; margin-top: 12px; border-top: 1px solid #eee; padding-top: 8px; }

    .order-card + .order-card { margin-top: 16px; }

    .items-box { background: #fdfdfd; border: 1px dashed #ddd;
             padding: 12px; margin: 15px 0; border-radius: 4px; }

    .items-box ul { list-style: none; padding-left: 5px; }
    
    .items-box li { color: #2d3436; font-weight: 500; font-size: 15px; }

    .status-form { margin-top: 15px; display: flex; align-items: flex-end; justify-content: space-between; gap: 10px; }

    .status-form form { display: flex;  align-items: flex-end; margin: 0; }

    .custom-select { padding: 6px 10px;  border-radius: 4px; border: 1px solid #ddd; font-size: 14px;
         background-color: #f9f9f9; cursor: pointer; margin-bottom: 15px; 
        -webkit-appearance: none; appearance: none; }

    .del-btn { margin-top: 15px; }

    .total-price { font-size: 18px; font-weight: 800; text-align: right; }

</style>
<body>
    <div class="container">
        <div class="editOrdertitle">
            <div class="title-row">
                <h1>後台管理</h1>
                <form action="/admin/logout" method="post">
                    <button class="back-btn">登出</button>
                </form>
            </div>
        </div>
        <div class="editOrdercontent" style="margin-top: 20px;">
            <% orders.forEach(order => { %>
                <div class="order-card">
                    <h2>訂單來源：<%= order.customer_name %></h2>
                    <p>電話：<%= order.phone %></p>
                    <p>配送地址：<%= order.address %></p>
                    <p>配送時間：<strong><%= order.delivery_time %></strong></p>

                    <h3>訂單內容：</h3>
                    <div class="items-box">
                        <ul>
                            <% order.items.forEach(item => { %>
                            <li><%= item.name %>：<%= item.qty %> 丁</li>
                            <% }) %>
                        </ul>
                    </div>

                    <div class="total-price">總金額：$<%= order.total_amount %></div>

                    <div class="status-form">
                        <form action="/tofu/<%= order.id %>/status?_method=PATCH" method="POST">
                            <select name="status" onchange="this.form.submit()" class="custom-select">
                                <option value="準備中" <%= order.status === '準備中' ? 'selected' : '' %>>準備中</option>
                                <option value="已出貨" <%= order.status === '已出貨' ? 'selected' : '' %>>已出貨</option>
                            </select>
                        </form>
                        <form action="/tofu/<%= order.id %>?_method=DELETE" method="POST">
                            <button type="submit" class="del-btn" 
                                    onclick="return confirm('確定刪除這筆訂單?')">刪除</button>
                        </form>
                    </div>
                    
                </div>
            <% }); %>
        </div>
    </div>
</body>
</html>