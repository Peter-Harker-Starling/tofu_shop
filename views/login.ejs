<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/Tofu.png" type="image/png" />
    <title>Login</title>
</head>
<style>
    * { margin: 0; padding: 0; box-sizing: border-box;
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "Noto Sans", Arial, sans-serif; }

    body { display: flex; justify-content: center; align-items: center; height: 100vh; }

    .login-card { background-color: #fff; padding: 32px; width: 320px;
        border-radius: 14px; box-shadow: 0 20px 40px rgba(0, 0, 0, 0.08); text-align: center; }

    .login-card h1 { margin-bottom: 25px; color: #222; font-size: 35px; }

    input { width: 100%; padding: 12px; margin-bottom: 20px; border: 1px solid #ddd;
        border-radius: 8px; font-size: 14px; transition: border-color 0.2s, box-shadow 0.2s; }

    input::placeholder { color: #aaa; }

    input:focus { border-color: #366bd6; box-shadow: 0 0 0 3px rgba(54, 107, 214, 0.15); }

    button { width: 100%; padding: 12px; background: linear-gradient(135deg, #4f7df3, #366bd6);
        color: white; border: none; border-radius: 8px; font-size: 15px; cursor: pointer; transition: transform 0.1s, box-shadow 0.1s; }

    button:hover { box-shadow: 0 8px 20px rgba(54, 107, 214, 0.35); transform: translateY(-1px); }

    button:active { transform: translateY(0); box-shadow: 0 4px 10px rgba(54, 107, 214, 0.25); }

    .error-message { color: #d9534f; margin-bottom: 15px; font-size: 14px; }
</style>
<body>
    <div class="login-card">
        <h1>Login</h1>
        <% if (error) { %>
            <script>alert('<%= error %>');</script>
        <% } %>
        <form action="/admin/login" method="post">
            <input type="text" name="username" placeholder="帳號" required />
            <input type="password" name="password" placeholder="密碼" required />
            <button type="submit">Login</button>
        </form>
    </div>
</body>
</html>